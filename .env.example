COMPOSE_PROJECT_NAME=ai-first
POSTGRES_DB=ai_first
POSTGRES_USER=ai_first
POSTGRES_PASSWORD=change-me
BACKEND_PORT=8010
FRONTEND_PORT=3001
# Empty: same-origin /api via frontend nginx to backend:8010.
# Set an absolute API origin only when using an external backend.
API_BASE=
